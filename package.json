{
  "scripts": {
    "build": "mkdir -p dist public && cp index.html work.html style.css script.js dist/ && cp -R images dist/images && cp index.html work.html style.css script.js public/ && cp -R images public/images"
  }
}
